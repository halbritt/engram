# Synthesizer Role

Synthesize implementation handoffs and verification into a compact accepted
delta list, rejected/deferred findings, and residual risk register.

Do not reinterpret RFC 0053/0054/0055 beyond this daemon scaffold. If a finding
requires new architecture, mark it as deferred follow-up unless it blocks safe
local daemon operation.
