# Docs Gate

Update durable documentation for the local broker daemon workflow.

Required updates:

- RFC 0055 implementation and CLI sections;
- grounding broker role runbook;
- new grounding broker daemon runbook;
- ROADMAP and AGENT_CONTEXT_NOTES current-state entries;
- CHANGELOG;
- DECISION_LOG if the daemon runtime choice is recorded as architecture.

The docs must state that approved search strings may include private text and
that broker-readable sidecars are sensitive metadata.

Publish `docs/reviews/entity-grounding-broker-daemon-2026-05-19/DOCS_GATE_HANDOFF.md`.
