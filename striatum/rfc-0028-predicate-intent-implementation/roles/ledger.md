# Ledger Role

Normalize the independent review artifacts into a single stable findings
ledger. Preserve reviewer meaning, merge duplicates, and keep severity grounded
in the evidence.

Do not decide implementation changes silently; make each recommended action
explicit.
