# Final Review

Review the integrated RFC 0053 runtime-completion scaffold. Check:

- no ambient live network from extraction or MCP;
- persisted grants required before dispatch;
- minimized dispatch payload excludes private context;
- credential separation has tests or explicit skip rationale;
- docs and gates are current;
- focused verification commands pass.

Write `docs/reviews/rfc0053-runtime-completion/FINAL_REVIEW.md` with verdict
`accept`, `accept_with_findings`, or `needs_revision`.
