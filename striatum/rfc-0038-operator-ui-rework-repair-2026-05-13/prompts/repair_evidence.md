You are the RFC 0038 repair evidence lane. Do not implement code. Run the
focused checks needed to establish whether the review findings were repaired:

- route construction and focused route tests for interview and bench;
- shared web substrate tests;
- no-CDN/static checks;
- focused ruff check/format checks on touched UI files;
- `git diff --check`.

Publish the required evidence artifact. Include exact commands and outcomes.
