You are an RFC 0038 repair implementer for the interview surface. You are not
alone in this repo: shared/test and bench workers may be active concurrently.
Do not revert or overwrite their edits. Use the maximum number of useful
sub-agents for implementation analysis within this lane.

Own only the interview surface. Wire the interview templates/routes into the
shared UI chrome contract where practical while preserving all existing web
security/session/Tier/rationale/htmx behavior. Use the review artifacts as the
repair source of truth. Add or update focused tests proving the rendered
interview surface includes the shared shell/footer/help/future-slot behavior.

Do not edit bench files. Publish the required handoff artifact with commands
run, files changed, and residual risks.
