# Adversarial Eval Review of Spec 0030

Apply the design-review eval lens to the *spec*. The spec must now
specify exact bench mechanics: how arms are run, how the gold set is
constructed, how the paired metric is computed, how thresholds are
applied.

Find any place where the spec softens the three-arm requirement,
loses the pre-registered threshold, under-specifies the gold set, or
breaks the baseline reproducibility check.

Output structure: same E### findings as the design-review eval
adversary review.

Do not modify the spec.
