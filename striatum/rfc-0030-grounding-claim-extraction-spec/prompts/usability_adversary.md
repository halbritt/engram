# Adversarial Usability Review of Spec 0030

Same lens as the design-review usability adversary, but applied to the
*spec*: concrete CLI argparse shapes, exact onboarding walkthrough,
default values, error messages.

Find places where a tired operator using the documented surface will
hit ambiguity, footguns, or excessive friction. Cite specific argparse
flags, exit codes, error strings.

Output structure: same U### findings as the design-review usability
adversary review.

Do not modify the spec.
