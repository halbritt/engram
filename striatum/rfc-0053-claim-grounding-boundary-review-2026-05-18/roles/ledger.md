# Ledger Role -- RFC 0053 Findings Ledger

Normalize all independent RFC 0053 reviews into a stable findings ledger.

Preserve the finding substance, highest reported severity, source lanes, and
affected artifact. Do not decide whether findings are accepted or rejected. Do
not rewrite the RFC. Write only the expected ledger artifact.
