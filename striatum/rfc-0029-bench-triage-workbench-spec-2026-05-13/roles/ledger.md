# Ledger Role

Normalize review findings from independent lanes into one stable findings
ledger. Preserve source lane provenance, de-duplicate overlapping findings, and
classify each finding by severity and disposition target.

The ledger does not apply changes. It makes the review set actionable.

