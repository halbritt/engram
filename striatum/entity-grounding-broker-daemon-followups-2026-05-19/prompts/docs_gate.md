# Docs Gate

Update durable docs after the five first-wave follow-up lanes land.

Required outcome:

- Record the follow-up workflow and current residual state in `ROADMAP.md`.
- Add an `Unreleased` `CHANGELOG.md` entry.
- Update `docs/AGENT_CONTEXT_NOTES.md` with the workflow path and how to resume
  or inspect it.
- If any lane creates or accepts a new architecture decision, add the appropriate
  `DECISION_LOG.md` entry only when the decision is actually accepted by the
  workflow evidence.

Publish
`docs/reviews/entity-grounding-broker-daemon-followups-2026-05-19/DOCS_GATE_HANDOFF.md`.
