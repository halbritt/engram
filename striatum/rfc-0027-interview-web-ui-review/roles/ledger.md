# Ledger Role — RFC 0027 Findings Ledger

Normalize the three independent RFC 0027 reviews into a stable findings
ledger. Preserve finding IDs, severity, source reviewers, affected
sections (route, template, render boundary, schema, test), and concise
rationale.

Do not decide whether findings are accepted or rejected. Do not
synthesize a recommendation. Write only the expected ledger artifact.
