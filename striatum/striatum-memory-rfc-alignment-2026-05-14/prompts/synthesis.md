# Final Synthesis Prompt

Synthesize the RFC alignment workflow ledger into an operator-ready closeout.
Do not promote RFCs, authorize implementation, or alter Striatum state.

Use read-only sub-agents where useful to classify remaining work. The synthesis
must state whether the alignment package is ready for a separate promotion
packet, what remains blocked before implementation, and what remains deferred
for personal memory or generated memory products.

Write only the expected synthesis artifact and run `git diff --check` for it.

