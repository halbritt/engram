Drafted as RFC 0004, augmenting 0003 rather than replacing it. The three substantive deltas: pull telemetry/progress out of the segmenter entirely (it’s supervisor-owned per 0001), rename “Orchestrator” to a single non-looping worker entry point, and add an explicit prompt builder module that materializes the versioned RequestProfile for D034 audit. Migration is staged in 6 independently revertable steps, with the request-profile stamp landing as JSON until the supervisor schema work decides on a normalized form.​​​​​​​​​​​​​​​​